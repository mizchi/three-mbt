// Generated by scripts/generate.mjs. Do not edit.
import { PerspectiveCamera, OrthographicCamera, Float32BufferAttribute, BufferGeometry, BoxGeometry, PlaneGeometry, SphereGeometry, CylinderGeometry, ConeGeometry, TorusGeometry, TorusKnotGeometry, CircleGeometry, RingGeometry, IcosahedronGeometry, CapsuleGeometry, EdgesGeometry, WireframeGeometry, MeshBasicMaterial, MeshStandardMaterial, MeshPhysicalMaterial, MeshPhongMaterial, MeshLambertMaterial, MeshNormalMaterial, LineBasicMaterial, PointsMaterial, SpriteMaterial, Mesh, SkinnedMesh, Bone, Skeleton, InstancedMesh, Line, LineSegments, Points, Sprite, AmbientLight, DirectionalLight, PointLight, SpotLight, HemisphereLight, Fog, FogExp2, Texture, DataTexture, PMREMGenerator, CanvasTexture, WebGLRenderTarget, AxesHelper, GridHelper, BoxHelper, CameraHelper, Box3, Sphere, Raycaster, NumberKeyframeTrack, VectorKeyframeTrack, QuaternionKeyframeTrack, AnimationClip, AnimationMixer, TextureLoader, LoadingManager, Vector4, Matrix3, Plane, Ray, Frustum, Spherical, Timer, Layers, DepthTexture, CubeTexture, WebGLCubeRenderTarget, InstancedBufferGeometry, InterleavedBufferAttribute, LineCurve, QuadraticBezierCurve, CubicBezierCurve, LineCurve3, QuadraticBezierCurve3, CubicBezierCurve3, SplineCurve, EllipseCurve, Path, Shape, ShapeGeometry, TubeGeometry, LatheGeometry, ExtrudeGeometry, CubeTextureLoader, Data3DTexture, DataArrayTexture, VideoTexture, MeshToonMaterial, MeshMatcapMaterial, MeshDepthMaterial, MeshDistanceMaterial, ShadowMaterial, BatchedMesh, CubeCamera, RectAreaLight, ArrowHelper, SkeletonHelper, Box3Helper, PlaneHelper, DirectionalLightHelper, PointLightHelper, SpotLightHelper, HemisphereLightHelper, Box2, Line3, Triangle, Cylindrical, SphericalHarmonics3, Matrix2, ArrayCamera, StereoCamera, LOD, LightProbe, ColorKeyframeTrack, BooleanKeyframeTrack, StringKeyframeTrack, AnimationObjectGroup, TetrahedronGeometry, OctahedronGeometry, DodecahedronGeometry, PolyhedronGeometry, LineLoop, LineDashedMaterial, Int8BufferAttribute, Int16BufferAttribute, Int32BufferAttribute, Uint8BufferAttribute, Uint8ClampedBufferAttribute, Uint16BufferAttribute, Uint32BufferAttribute, Float16BufferAttribute, ObjectLoader, BufferGeometryLoader, MaterialLoader, AnimationLoader, FileLoader, ArcCurve, ShapePath, Source, UniformsGroup, FramebufferTexture, WebGL3DRenderTarget, WebGLArrayRenderTarget } from 'three';

export const perspectiveCamera = (fov, aspect, near, far) => new PerspectiveCamera(fov, aspect, near, far);
export const orthographicCamera = (left, right, top, bottom, near, far) => new OrthographicCamera(left, right, top, bottom, near, far);
export const float32BufferAttribute = (values, item_size) => new Float32BufferAttribute(values, item_size);
export const bufferGeometry = () => new BufferGeometry();
export const boxGeometry = (width, height, depth) => new BoxGeometry(width, height, depth);
export const BoxGeometry_segmented = (width, height, depth, width_segments, height_segments, depth_segments) => new BoxGeometry(width, height, depth, width_segments, height_segments, depth_segments);
export const planeGeometry = (width, height) => new PlaneGeometry(width, height);
export const PlaneGeometry_segmented = (width, height, width_segments, height_segments) => new PlaneGeometry(width, height, width_segments, height_segments);
export const sphereGeometry = (radius, width_segments, height_segments) => new SphereGeometry(radius, width_segments, height_segments);
export const SphereGeometry_section = (radius, width_segments, height_segments, phi_start, phi_length, theta_start, theta_length) => new SphereGeometry(radius, width_segments, height_segments, phi_start, phi_length, theta_start, theta_length);
export const cylinderGeometry = (radius_top, radius_bottom, height, radial_segments) => new CylinderGeometry(radius_top, radius_bottom, height, radial_segments);
export const CylinderGeometry_section = (radius_top, radius_bottom, height, radial_segments, height_segments, open_ended, theta_start, theta_length) => new CylinderGeometry(radius_top, radius_bottom, height, radial_segments, height_segments, open_ended, theta_start, theta_length);
export const coneGeometry = (radius, height, radial_segments) => new ConeGeometry(radius, height, radial_segments);
export const ConeGeometry_section = (radius, height, radial_segments, height_segments, open_ended, theta_start, theta_length) => new ConeGeometry(radius, height, radial_segments, height_segments, open_ended, theta_start, theta_length);
export const torusGeometry = (radius, tube, radial_segments, tubular_segments) => new TorusGeometry(radius, tube, radial_segments, tubular_segments);
export const TorusGeometry_arc = (radius, tube, radial_segments, tubular_segments, arc) => new TorusGeometry(radius, tube, radial_segments, tubular_segments, arc);
export const torusKnotGeometry = (radius, tube, tubular_segments, radial_segments) => new TorusKnotGeometry(radius, tube, tubular_segments, radial_segments);
export const TorusKnotGeometry_with_winding = (radius, tube, tubular_segments, radial_segments, p, q) => new TorusKnotGeometry(radius, tube, tubular_segments, radial_segments, p, q);
export const circleGeometry = (radius, segments) => new CircleGeometry(radius, segments);
export const CircleGeometry_arc = (radius, segments, theta_start, theta_length) => new CircleGeometry(radius, segments, theta_start, theta_length);
export const ringGeometry = (inner_radius, outer_radius, theta_segments) => new RingGeometry(inner_radius, outer_radius, theta_segments);
export const RingGeometry_section = (inner_radius, outer_radius, theta_segments, phi_segments, theta_start, theta_length) => new RingGeometry(inner_radius, outer_radius, theta_segments, phi_segments, theta_start, theta_length);
export const icosahedronGeometry = (radius, detail) => new IcosahedronGeometry(radius, detail);
export const capsuleGeometry = (radius, height, cap_segments, radial_segments) => new CapsuleGeometry(radius, height, cap_segments, radial_segments);
export const edgesGeometry = (geometry, threshold_angle) => new EdgesGeometry(geometry, threshold_angle);
export const wireframeGeometry = (geometry) => new WireframeGeometry(geometry);
export const meshBasicMaterial = (color) => new MeshBasicMaterial({ color });
export const meshStandardMaterial = (color, roughness, metalness) => new MeshStandardMaterial({ color, roughness, metalness });
export const meshPhysicalMaterial = (color, roughness, metalness) => new MeshPhysicalMaterial({ color, roughness, metalness });
export const meshPhongMaterial = (color, shininess) => new MeshPhongMaterial({ color, shininess });
export const meshLambertMaterial = (color) => new MeshLambertMaterial({ color });
export const meshNormalMaterial = () => new MeshNormalMaterial({  });
export const lineBasicMaterial = (color) => new LineBasicMaterial({ color });
export const pointsMaterial = (color, size) => new PointsMaterial({ color, size });
export const spriteMaterial = (color) => new SpriteMaterial({ color });
export const mesh = (geometry, material) => new Mesh(geometry, material);
export const skinnedMesh = (geometry, material) => new SkinnedMesh(geometry, material);
export const bone = () => new Bone();
export const skeleton = (bones) => new Skeleton(bones);
export const instancedMesh = (geometry, material, count) => new InstancedMesh(geometry, material, count);
export const line = (geometry, material) => new Line(geometry, material);
export const lineSegments = (geometry, material) => new LineSegments(geometry, material);
export const points = (geometry, material) => new Points(geometry, material);
export const sprite = (material) => new Sprite(material);
export const ambientLight = (color, intensity) => new AmbientLight(color, intensity);
export const directionalLight = (color, intensity) => new DirectionalLight(color, intensity);
export const pointLight = (color, intensity, distance, decay) => new PointLight(color, intensity, distance, decay);
export const spotLight = (color, intensity, distance, angle, penumbra, decay) => new SpotLight(color, intensity, distance, angle, penumbra, decay);
export const hemisphereLight = (sky_color, ground_color, intensity) => new HemisphereLight(sky_color, ground_color, intensity);
export const fog = (color, near, far) => new Fog(color, near, far);
export const fogExp2 = (color, density) => new FogExp2(color, density);
export const texture = () => new Texture();
export const dataTextureRGBA = (data, width, height) => new DataTexture(data, width, height);
export const pMREMGenerator = (renderer) => new PMREMGenerator(renderer);
export const canvasTexture = (canvas) => new CanvasTexture(canvas);
export const webGLRenderTarget = (width, height) => new WebGLRenderTarget(width, height);
export const axesHelper = (size) => new AxesHelper(size);
export const gridHelper = (size, divisions) => new GridHelper(size, divisions);
export const boxHelper = (object, color) => new BoxHelper(object, color);
export const cameraHelper = (camera) => new CameraHelper(camera);
export const box3 = () => new Box3();
export const sphere = (center, radius) => new Sphere(center, radius);
export const raycaster = (origin, direction) => new Raycaster(origin, direction);
export const numberKeyframeTrack = (name, times, values) => new NumberKeyframeTrack(name, times, values);
export const vectorKeyframeTrack = (name, times, values) => new VectorKeyframeTrack(name, times, values);
export const quaternionKeyframeTrack = (name, times, values) => new QuaternionKeyframeTrack(name, times, values);
export const animationClip = (name, duration, tracks) => new AnimationClip(name, duration, tracks);
export const animationMixer = (root) => new AnimationMixer(root);
export const textureLoader = () => new TextureLoader();
export const loadingManager = () => new LoadingManager();
export const vector4 = (x, y, z, w) => new Vector4(x, y, z, w);
export const matrix3 = () => new Matrix3();
export const plane = (normal, constant) => new Plane(normal, constant);
export const ray = (origin, direction) => new Ray(origin, direction);
export const frustum = () => new Frustum();
export const spherical = (radius, phi, theta) => new Spherical(radius, phi, theta);
export const timer = () => new Timer();
export const layers = () => new Layers();
export const depthTexture = (width, height) => new DepthTexture(width, height);
export const cubeTexture = () => new CubeTexture();
export const webGLCubeRenderTarget = (size) => new WebGLCubeRenderTarget(size);
export const instancedBufferGeometry = () => new InstancedBufferGeometry();
export const interleavedBufferAttribute = (data, item_size, offset, normalized) => new InterleavedBufferAttribute(data, item_size, offset, normalized);
export const lineCurve = (start, end) => new LineCurve(start, end);
export const quadraticBezierCurve = (start, control, end) => new QuadraticBezierCurve(start, control, end);
export const cubicBezierCurve = (start, control1, control2, end) => new CubicBezierCurve(start, control1, control2, end);
export const lineCurve3 = (start, end) => new LineCurve3(start, end);
export const quadraticBezierCurve3 = (start, control, end) => new QuadraticBezierCurve3(start, control, end);
export const cubicBezierCurve3 = (start, control1, control2, end) => new CubicBezierCurve3(start, control1, control2, end);
export const splineCurve = (points) => new SplineCurve(points);
export const ellipseCurve = (x, y, radius_x, radius_y, start_angle, end_angle, clockwise, rotation) => new EllipseCurve(x, y, radius_x, radius_y, start_angle, end_angle, clockwise, rotation);
export const path = () => new Path();
export const Path_from_points = (points) => new Path(points);
export const shape = () => new Shape();
export const Shape_from_points = (points) => new Shape(points);
export const shapeGeometry = (shapes, curve_segments) => new ShapeGeometry(shapes, curve_segments);
export const tubeGeometry = (path, tubular_segments, radius, radial_segments, closed) => new TubeGeometry(path, tubular_segments, radius, radial_segments, closed);
export const latheGeometry = (points, segments, phi_start, phi_length) => new LatheGeometry(points, segments, phi_start, phi_length);
export const extrudeGeometry = (shapes, options) => new ExtrudeGeometry(shapes, options);
export const cubeTextureLoader = () => new CubeTextureLoader();
export const data3DTexture = (bytes, width, height, depth) => new Data3DTexture(bytes, width, height, depth);
export const dataArrayTexture = (bytes, width, height, depth) => new DataArrayTexture(bytes, width, height, depth);
export const videoTexture = (video) => new VideoTexture(video);
export const meshToonMaterial = (color) => new MeshToonMaterial({ color });
export const meshMatcapMaterial = (color) => new MeshMatcapMaterial({ color });
export const meshDepthMaterial = () => new MeshDepthMaterial({  });
export const meshDistanceMaterial = () => new MeshDistanceMaterial({  });
export const shadowMaterial = () => new ShadowMaterial({  });
export const batchedMesh = (max_instances, max_vertices, max_indices, material) => new BatchedMesh(max_instances, max_vertices, max_indices, material);
export const cubeCamera = (near, far, target) => new CubeCamera(near, far, target);
export const rectAreaLight = (color, intensity, width, height) => new RectAreaLight(color, intensity, width, height);
export const arrowHelper = (direction, origin, length, color) => new ArrowHelper(direction, origin, length, color);
export const skeletonHelper = (root) => new SkeletonHelper(root);
export const box3Helper = (box, color) => new Box3Helper(box, color);
export const planeHelper = (plane, size, color) => new PlaneHelper(plane, size, color);
export const directionalLightHelper = (light, size) => new DirectionalLightHelper(light, size);
export const pointLightHelper = (light, size) => new PointLightHelper(light, size);
export const spotLightHelper = (light) => new SpotLightHelper(light);
export const hemisphereLightHelper = (light, size) => new HemisphereLightHelper(light, size);
export const box2 = () => new Box2();
export const line3 = (start, end) => new Line3(start, end);
export const triangle = (a, b, c) => new Triangle(a, b, c);
export const cylindrical = (radius, theta, y) => new Cylindrical(radius, theta, y);
export const sphericalHarmonics3 = () => new SphericalHarmonics3();
export const matrix2 = () => new Matrix2();
export const arrayCamera = (cameras) => new ArrayCamera(cameras);
export const stereoCamera = () => new StereoCamera();
export const lOD = () => new LOD();
export const lightProbe = (sh, intensity) => new LightProbe(sh, intensity);
export const colorKeyframeTrack = (name, times, values) => new ColorKeyframeTrack(name, times, values);
export const booleanKeyframeTrack = (name, times, values) => new BooleanKeyframeTrack(name, times, values);
export const stringKeyframeTrack = (name, times, values) => new StringKeyframeTrack(name, times, values);
export const animationObjectGroup = () => new AnimationObjectGroup();
export const tetrahedronGeometry = (radius, detail) => new TetrahedronGeometry(radius, detail);
export const octahedronGeometry = (radius, detail) => new OctahedronGeometry(radius, detail);
export const dodecahedronGeometry = (radius, detail) => new DodecahedronGeometry(radius, detail);
export const polyhedronGeometry = (vertices, indices, radius, detail) => new PolyhedronGeometry(vertices, indices, radius, detail);
export const lineLoop = (geometry, material) => new LineLoop(geometry, material);
export const lineDashedMaterial = (color, scale, dash_size, gap_size) => new LineDashedMaterial({ color, scale, dashSize: dash_size, gapSize: gap_size });
export const typedInt8BufferAttribute = (values, item_size, normalized) => new Int8BufferAttribute(values, item_size, normalized);
export const typedInt16BufferAttribute = (values, item_size, normalized) => new Int16BufferAttribute(values, item_size, normalized);
export const typedInt32BufferAttribute = (values, item_size, normalized) => new Int32BufferAttribute(values, item_size, normalized);
export const typedUint8BufferAttribute = (values, item_size, normalized) => new Uint8BufferAttribute(values, item_size, normalized);
export const typedUint8ClampedBufferAttribute = (values, item_size, normalized) => new Uint8ClampedBufferAttribute(values, item_size, normalized);
export const typedUint16BufferAttribute = (values, item_size, normalized) => new Uint16BufferAttribute(values, item_size, normalized);
export const typedUint32BufferAttribute = (values, item_size, normalized) => new Uint32BufferAttribute(values, item_size, normalized);
export const typedFloat16BufferAttribute = (values, item_size, normalized) => new Float16BufferAttribute(values, item_size, normalized);
export const typedFloat32BufferAttribute = (values, item_size, normalized) => new Float32BufferAttribute(values, item_size, normalized);
export const objectLoader = () => new ObjectLoader();
export const bufferGeometryLoader = () => new BufferGeometryLoader();
export const materialLoader = () => new MaterialLoader();
export const animationLoader = () => new AnimationLoader();
export const fileLoader = () => new FileLoader();
export const arcCurve = (x, y, radius, start_angle, end_angle, clockwise) => new ArcCurve(x, y, radius, start_angle, end_angle, clockwise);
export const shapePath = () => new ShapePath();
export const source = () => new Source();
export const uniformsGroup = () => new UniformsGroup();
export const framebufferTexture = (width, height) => new FramebufferTexture(width, height);
export const webGL3DRenderTarget = (width, height, depth) => new WebGL3DRenderTarget(width, height, depth);
export const webGLArrayRenderTarget = (width, height, depth) => new WebGLArrayRenderTarget(width, height, depth);
