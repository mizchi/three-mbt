// Generated by scripts/generate.mjs from bindings/api.mjs. Do not edit.

///|
#external
pub type VertexTangentsHelper

///|
#module("@mizchi/three-mbt/modeling-edit")
pub extern "js" fn VertexTangentsHelper::VertexTangentsHelper(
  object : Object3D,
  size : Double,
  color : Int,
) -> VertexTangentsHelper = "vertexTangentsHelper"

///|
pub fn VertexTangentsHelper::as_object3d(
  self : VertexTangentsHelper,
) -> Object3D = "%identity"

///|
pub fn VertexTangentsHelper::as_line_segments(
  self : VertexTangentsHelper,
) -> LineSegments = "%identity"

///|
pub extern "js" fn VertexTangentsHelper::object(
  self : VertexTangentsHelper,
) -> Object3D =
  #| (self) => self.object

///|
pub extern "js" fn VertexTangentsHelper::size(
  self : VertexTangentsHelper,
) -> Double =
  #| (self) => self.size

///|
pub extern "js" fn VertexTangentsHelper::set_size(
  self : VertexTangentsHelper,
  value : Double,
) -> Unit =
  #| (self, value) => { self.size = value; }

///|
pub extern "js" fn VertexTangentsHelper::update(
  self : VertexTangentsHelper,
) -> Unit =
  #| (self) => self.update()

///|
pub extern "js" fn VertexTangentsHelper::dispose(
  self : VertexTangentsHelper,
) -> Unit =
  #| (self) => self.dispose()

///|
pub extern "js" fn VertexTangentsHelper::cast_shadow(
  self : VertexTangentsHelper,
) -> Bool =
  #| (self) => self.castShadow

///|
pub extern "js" fn VertexTangentsHelper::set_cast_shadow(
  self : VertexTangentsHelper,
  value : Bool,
) -> Unit =
  #| (self, value) => { self.castShadow = value; }

///|
pub extern "js" fn VertexTangentsHelper::receive_shadow(
  self : VertexTangentsHelper,
) -> Bool =
  #| (self) => self.receiveShadow

///|
pub extern "js" fn VertexTangentsHelper::set_receive_shadow(
  self : VertexTangentsHelper,
  value : Bool,
) -> Unit =
  #| (self, value) => { self.receiveShadow = value; }

///|
pub extern "js" fn VertexTangentsHelper::id(self : VertexTangentsHelper) -> Int =
  #| (self) => self.id

///|
pub extern "js" fn VertexTangentsHelper::uuid(
  self : VertexTangentsHelper,
) -> String =
  #| (self) => self.uuid

///|
pub extern "js" fn VertexTangentsHelper::kind(
  self : VertexTangentsHelper,
) -> String =
  #| (self) => self.type

///|
pub extern "js" fn VertexTangentsHelper::name(
  self : VertexTangentsHelper,
) -> String =
  #| (self) => self.name

///|
pub extern "js" fn VertexTangentsHelper::visible(
  self : VertexTangentsHelper,
) -> Bool =
  #| (self) => self.visible

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::position(
  self : VertexTangentsHelper,
) -> Vector3 =
  #| (self) => self.position

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::rotation(
  self : VertexTangentsHelper,
) -> Euler =
  #| (self) => self.rotation

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::quaternion(
  self : VertexTangentsHelper,
) -> Quaternion =
  #| (self) => self.quaternion

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::scale(
  self : VertexTangentsHelper,
) -> Vector3 =
  #| (self) => self.scale

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::up(
  self : VertexTangentsHelper,
) -> Vector3 =
  #| (self) => self.up

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::matrix(
  self : VertexTangentsHelper,
) -> Matrix4 =
  #| (self) => self.matrix

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn VertexTangentsHelper::matrix_world(
  self : VertexTangentsHelper,
) -> Matrix4 =
  #| (self) => self.matrixWorld

///|
pub extern "js" fn VertexTangentsHelper::set_name(
  self : VertexTangentsHelper,
  name : String,
) -> Unit =
  #| (self, name) => { self.name = name; }

///|
pub extern "js" fn VertexTangentsHelper::set_visible(
  self : VertexTangentsHelper,
  visible : Bool,
) -> Unit =
  #| (self, visible) => { self.visible = visible; }

///|
pub extern "js" fn VertexTangentsHelper::children(
  self : VertexTangentsHelper,
) -> FixedArray[Object3D] =
  #| (self) => self.children.slice()

///|
pub extern "js" fn VertexTangentsHelper::add(
  self : VertexTangentsHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.add(child)

///|
pub extern "js" fn VertexTangentsHelper::remove(
  self : VertexTangentsHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.remove(child)

///|
pub extern "js" fn VertexTangentsHelper::attach(
  self : VertexTangentsHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.attach(child)

///|
pub extern "js" fn VertexTangentsHelper::remove_from_parent(
  self : VertexTangentsHelper,
) -> Object3D =
  #| (self) => self.removeFromParent()

///|
pub extern "js" fn VertexTangentsHelper::look_at(
  self : VertexTangentsHelper,
  target : Vector3,
) -> Unit =
  #| (self, target) => self.lookAt(target)

///|
pub extern "js" fn VertexTangentsHelper::rotate_x(
  self : VertexTangentsHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateX(angle)

///|
pub extern "js" fn VertexTangentsHelper::rotate_y(
  self : VertexTangentsHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateY(angle)

///|
pub extern "js" fn VertexTangentsHelper::rotate_z(
  self : VertexTangentsHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateZ(angle)

///|
pub extern "js" fn VertexTangentsHelper::translate_x(
  self : VertexTangentsHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateX(distance)

///|
pub extern "js" fn VertexTangentsHelper::translate_y(
  self : VertexTangentsHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateY(distance)

///|
pub extern "js" fn VertexTangentsHelper::translate_z(
  self : VertexTangentsHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateZ(distance)

///|
pub extern "js" fn VertexTangentsHelper::apply_matrix4(
  self : VertexTangentsHelper,
  matrix : Matrix4,
) -> Unit =
  #| (self, matrix) => self.applyMatrix4(matrix)

///|
pub extern "js" fn VertexTangentsHelper::apply_quaternion(
  self : VertexTangentsHelper,
  quaternion : Quaternion,
) -> Object3D =
  #| (self, quaternion) => self.applyQuaternion(quaternion)

///|
pub extern "js" fn VertexTangentsHelper::update_matrix(
  self : VertexTangentsHelper,
) -> Unit =
  #| (self) => self.updateMatrix()

///|
pub extern "js" fn VertexTangentsHelper::update_matrix_world(
  self : VertexTangentsHelper,
  force : Bool,
) -> Unit =
  #| (self, force) => self.updateMatrixWorld(force)

///|
pub extern "js" fn VertexTangentsHelper::update_world_matrix(
  self : VertexTangentsHelper,
  update_parents : Bool,
  update_children : Bool,
) -> Unit =
  #| (self, update_parents, update_children) => self.updateWorldMatrix(update_parents, update_children)

///|
pub extern "js" fn VertexTangentsHelper::get_world_position(
  self : VertexTangentsHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldPosition(target)

///|
pub extern "js" fn VertexTangentsHelper::get_world_quaternion(
  self : VertexTangentsHelper,
  target : Quaternion,
) -> Quaternion =
  #| (self, target) => self.getWorldQuaternion(target)

///|
pub extern "js" fn VertexTangentsHelper::get_world_scale(
  self : VertexTangentsHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldScale(target)

///|
pub extern "js" fn VertexTangentsHelper::get_world_direction(
  self : VertexTangentsHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldDirection(target)

///|
pub extern "js" fn VertexTangentsHelper::local_to_world(
  self : VertexTangentsHelper,
  vector : Vector3,
) -> Vector3 =
  #| (self, vector) => self.localToWorld(vector)

///|
pub extern "js" fn VertexTangentsHelper::world_to_local(
  self : VertexTangentsHelper,
  vector : Vector3,
) -> Vector3 =
  #| (self, vector) => self.worldToLocal(vector)

///|
pub extern "js" fn VertexTangentsHelper::traverse(
  self : VertexTangentsHelper,
  callback : (Object3D) -> Unit,
) -> Unit =
  #| (self, callback) => self.traverse(callback)

///|
pub extern "js" fn VertexTangentsHelper::traverse_visible(
  self : VertexTangentsHelper,
  callback : (Object3D) -> Unit,
) -> Unit =
  #| (self, callback) => self.traverseVisible(callback)

///|
/// Copies coordinates into the live position vector; does not retain the input vector.
pub extern "js" fn VertexTangentsHelper::set_position(
  self : VertexTangentsHelper,
  position : Vector3,
) -> Unit =
  #| (self, position) => { self.position.copy(position); }

///|
/// Sets local position coordinates while preserving the live position reference.
pub extern "js" fn VertexTangentsHelper::set_position_xyz(
  self : VertexTangentsHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.position.set(x, y, z); }

///|
/// Copies Euler angles and order; native quaternion synchronization is preserved.
pub extern "js" fn VertexTangentsHelper::set_rotation(
  self : VertexTangentsHelper,
  rotation : Euler,
) -> Unit =
  #| (self, rotation) => { self.rotation.copy(rotation); }

///|
/// Sets local Euler angles in radians, retaining the current Euler order.
pub extern "js" fn VertexTangentsHelper::set_rotation_xyz(
  self : VertexTangentsHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.rotation.set(x, y, z); }

///|
/// Copies quaternion components; native Euler synchronization is preserved. Does not normalize.
pub extern "js" fn VertexTangentsHelper::set_quaternion(
  self : VertexTangentsHelper,
  quaternion : Quaternion,
) -> Unit =
  #| (self, quaternion) => { self.quaternion.copy(quaternion); }

///|
/// Copies local scale components into the existing live scale vector.
pub extern "js" fn VertexTangentsHelper::set_scale(
  self : VertexTangentsHelper,
  scale : Vector3,
) -> Unit =
  #| (self, scale) => { self.scale.copy(scale); }

///|
/// Sets the three local scale components.
pub extern "js" fn VertexTangentsHelper::set_scale_xyz(
  self : VertexTangentsHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.scale.set(x, y, z); }

///|
/// Sets all local scale components to the supplied value.
pub extern "js" fn VertexTangentsHelper::set_uniform_scale(
  self : VertexTangentsHelper,
  scale : Double,
) -> Unit =
  #| (self, scale) => { self.scale.setScalar(scale); }

///|
/// Adds a child using native reparenting and event behavior.
pub extern "js" fn VertexTangentsHelper::add_child(
  self : VertexTangentsHelper,
  child : Object3D,
) -> Unit =
  #| (self, child) => { self.add(child); }

///|
/// Adds children in order using native reparenting and event behavior. An empty list is a no-op.
pub extern "js" fn VertexTangentsHelper::add_children(
  self : VertexTangentsHelper,
  children : FixedArray[Object3D],
) -> Unit =
  #| (self, children) => { if (children.length > 0) { self.add(...children); }; }

///|
/// Adds this object to the parent, retaining this object as the cascade receiver.
pub extern "js" fn VertexTangentsHelper::add_to(
  self : VertexTangentsHelper,
  parent : Object3D,
) -> Unit =
  #| (self, parent) => { parent.add(self); }

///|
/// Removes a child using native event behavior. Does not dispose resources.
pub extern "js" fn VertexTangentsHelper::remove_child(
  self : VertexTangentsHelper,
  child : Object3D,
) -> Unit =
  #| (self, child) => { self.remove(child); }
