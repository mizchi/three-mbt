// Generated by scripts/generate.mjs from bindings/api.mjs. Do not edit.

///|
#external
pub type ArrowHelper

///|
#module("@mizchi/three-mbt/constructors")
pub extern "js" fn ArrowHelper::ArrowHelper(
  direction : Vector3,
  origin : Vector3,
  length : Double,
  color : Int,
) -> ArrowHelper = "arrowHelper"

///|
pub fn ArrowHelper::as_object3d(self : ArrowHelper) -> Object3D = "%identity"

///|
pub extern "js" fn ArrowHelper::set_direction(
  self : ArrowHelper,
  direction : Vector3,
) -> Unit =
  #| (self, direction) => self.setDirection(direction)

///|
pub extern "js" fn ArrowHelper::set_length(
  self : ArrowHelper,
  length : Double,
  head_length : Double,
  head_width : Double,
) -> Unit =
  #| (self, length, head_length, head_width) => self.setLength(length, head_length, head_width)

///|
pub extern "js" fn ArrowHelper::set_color(
  self : ArrowHelper,
  color : Color,
) -> Unit =
  #| (self, color) => self.setColor(color)

///|
pub extern "js" fn ArrowHelper::dispose(self : ArrowHelper) -> Unit =
  #| (self) => self.dispose()

///|
pub extern "js" fn ArrowHelper::cast_shadow(self : ArrowHelper) -> Bool =
  #| (self) => self.castShadow

///|
pub extern "js" fn ArrowHelper::set_cast_shadow(
  self : ArrowHelper,
  value : Bool,
) -> Unit =
  #| (self, value) => { self.castShadow = value; }

///|
pub extern "js" fn ArrowHelper::receive_shadow(self : ArrowHelper) -> Bool =
  #| (self) => self.receiveShadow

///|
pub extern "js" fn ArrowHelper::set_receive_shadow(
  self : ArrowHelper,
  value : Bool,
) -> Unit =
  #| (self, value) => { self.receiveShadow = value; }

///|
pub extern "js" fn ArrowHelper::id(self : ArrowHelper) -> Int =
  #| (self) => self.id

///|
pub extern "js" fn ArrowHelper::uuid(self : ArrowHelper) -> String =
  #| (self) => self.uuid

///|
pub extern "js" fn ArrowHelper::kind(self : ArrowHelper) -> String =
  #| (self) => self.type

///|
pub extern "js" fn ArrowHelper::name(self : ArrowHelper) -> String =
  #| (self) => self.name

///|
pub extern "js" fn ArrowHelper::visible(self : ArrowHelper) -> Bool =
  #| (self) => self.visible

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::position(self : ArrowHelper) -> Vector3 =
  #| (self) => self.position

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::rotation(self : ArrowHelper) -> Euler =
  #| (self) => self.rotation

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::quaternion(self : ArrowHelper) -> Quaternion =
  #| (self) => self.quaternion

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::scale(self : ArrowHelper) -> Vector3 =
  #| (self) => self.scale

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::up(self : ArrowHelper) -> Vector3 =
  #| (self) => self.up

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::matrix(self : ArrowHelper) -> Matrix4 =
  #| (self) => self.matrix

///|
/// Returns the live three.js reference, not a copy.
pub extern "js" fn ArrowHelper::matrix_world(self : ArrowHelper) -> Matrix4 =
  #| (self) => self.matrixWorld

///|
pub extern "js" fn ArrowHelper::set_name(
  self : ArrowHelper,
  name : String,
) -> Unit =
  #| (self, name) => { self.name = name; }

///|
pub extern "js" fn ArrowHelper::set_visible(
  self : ArrowHelper,
  visible : Bool,
) -> Unit =
  #| (self, visible) => { self.visible = visible; }

///|
pub extern "js" fn ArrowHelper::children(
  self : ArrowHelper,
) -> FixedArray[Object3D] =
  #| (self) => self.children.slice()

///|
pub extern "js" fn ArrowHelper::add(
  self : ArrowHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.add(child)

///|
pub extern "js" fn ArrowHelper::remove(
  self : ArrowHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.remove(child)

///|
pub extern "js" fn ArrowHelper::attach(
  self : ArrowHelper,
  child : Object3D,
) -> Object3D =
  #| (self, child) => self.attach(child)

///|
pub extern "js" fn ArrowHelper::remove_from_parent(
  self : ArrowHelper,
) -> Object3D =
  #| (self) => self.removeFromParent()

///|
pub extern "js" fn ArrowHelper::look_at(
  self : ArrowHelper,
  target : Vector3,
) -> Unit =
  #| (self, target) => self.lookAt(target)

///|
pub extern "js" fn ArrowHelper::rotate_x(
  self : ArrowHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateX(angle)

///|
pub extern "js" fn ArrowHelper::rotate_y(
  self : ArrowHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateY(angle)

///|
pub extern "js" fn ArrowHelper::rotate_z(
  self : ArrowHelper,
  angle : Double,
) -> Object3D =
  #| (self, angle) => self.rotateZ(angle)

///|
pub extern "js" fn ArrowHelper::translate_x(
  self : ArrowHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateX(distance)

///|
pub extern "js" fn ArrowHelper::translate_y(
  self : ArrowHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateY(distance)

///|
pub extern "js" fn ArrowHelper::translate_z(
  self : ArrowHelper,
  distance : Double,
) -> Object3D =
  #| (self, distance) => self.translateZ(distance)

///|
pub extern "js" fn ArrowHelper::apply_matrix4(
  self : ArrowHelper,
  matrix : Matrix4,
) -> Unit =
  #| (self, matrix) => self.applyMatrix4(matrix)

///|
pub extern "js" fn ArrowHelper::apply_quaternion(
  self : ArrowHelper,
  quaternion : Quaternion,
) -> Object3D =
  #| (self, quaternion) => self.applyQuaternion(quaternion)

///|
pub extern "js" fn ArrowHelper::update_matrix(self : ArrowHelper) -> Unit =
  #| (self) => self.updateMatrix()

///|
pub extern "js" fn ArrowHelper::update_matrix_world(
  self : ArrowHelper,
  force : Bool,
) -> Unit =
  #| (self, force) => self.updateMatrixWorld(force)

///|
pub extern "js" fn ArrowHelper::update_world_matrix(
  self : ArrowHelper,
  update_parents : Bool,
  update_children : Bool,
) -> Unit =
  #| (self, update_parents, update_children) => self.updateWorldMatrix(update_parents, update_children)

///|
pub extern "js" fn ArrowHelper::get_world_position(
  self : ArrowHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldPosition(target)

///|
pub extern "js" fn ArrowHelper::get_world_quaternion(
  self : ArrowHelper,
  target : Quaternion,
) -> Quaternion =
  #| (self, target) => self.getWorldQuaternion(target)

///|
pub extern "js" fn ArrowHelper::get_world_scale(
  self : ArrowHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldScale(target)

///|
pub extern "js" fn ArrowHelper::get_world_direction(
  self : ArrowHelper,
  target : Vector3,
) -> Vector3 =
  #| (self, target) => self.getWorldDirection(target)

///|
pub extern "js" fn ArrowHelper::local_to_world(
  self : ArrowHelper,
  vector : Vector3,
) -> Vector3 =
  #| (self, vector) => self.localToWorld(vector)

///|
pub extern "js" fn ArrowHelper::world_to_local(
  self : ArrowHelper,
  vector : Vector3,
) -> Vector3 =
  #| (self, vector) => self.worldToLocal(vector)

///|
pub extern "js" fn ArrowHelper::traverse(
  self : ArrowHelper,
  callback : (Object3D) -> Unit,
) -> Unit =
  #| (self, callback) => self.traverse(callback)

///|
pub extern "js" fn ArrowHelper::traverse_visible(
  self : ArrowHelper,
  callback : (Object3D) -> Unit,
) -> Unit =
  #| (self, callback) => self.traverseVisible(callback)

///|
/// Copies coordinates into the live position vector; does not retain the input vector.
pub extern "js" fn ArrowHelper::set_position(
  self : ArrowHelper,
  position : Vector3,
) -> Unit =
  #| (self, position) => { self.position.copy(position); }

///|
/// Sets local position coordinates while preserving the live position reference.
pub extern "js" fn ArrowHelper::set_position_xyz(
  self : ArrowHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.position.set(x, y, z); }

///|
/// Copies Euler angles and order; native quaternion synchronization is preserved.
pub extern "js" fn ArrowHelper::set_rotation(
  self : ArrowHelper,
  rotation : Euler,
) -> Unit =
  #| (self, rotation) => { self.rotation.copy(rotation); }

///|
/// Sets local Euler angles in radians, retaining the current Euler order.
pub extern "js" fn ArrowHelper::set_rotation_xyz(
  self : ArrowHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.rotation.set(x, y, z); }

///|
/// Copies quaternion components; native Euler synchronization is preserved. Does not normalize.
pub extern "js" fn ArrowHelper::set_quaternion(
  self : ArrowHelper,
  quaternion : Quaternion,
) -> Unit =
  #| (self, quaternion) => { self.quaternion.copy(quaternion); }

///|
/// Copies local scale components into the existing live scale vector.
pub extern "js" fn ArrowHelper::set_scale(
  self : ArrowHelper,
  scale : Vector3,
) -> Unit =
  #| (self, scale) => { self.scale.copy(scale); }

///|
/// Sets the three local scale components.
pub extern "js" fn ArrowHelper::set_scale_xyz(
  self : ArrowHelper,
  x : Double,
  y : Double,
  z : Double,
) -> Unit =
  #| (self, x, y, z) => { self.scale.set(x, y, z); }

///|
/// Sets all local scale components to the supplied value.
pub extern "js" fn ArrowHelper::set_uniform_scale(
  self : ArrowHelper,
  scale : Double,
) -> Unit =
  #| (self, scale) => { self.scale.setScalar(scale); }

///|
/// Adds a child using native reparenting and event behavior.
pub extern "js" fn ArrowHelper::add_child(
  self : ArrowHelper,
  child : Object3D,
) -> Unit =
  #| (self, child) => { self.add(child); }

///|
/// Adds children in order using native reparenting and event behavior. An empty list is a no-op.
pub extern "js" fn ArrowHelper::add_children(
  self : ArrowHelper,
  children : FixedArray[Object3D],
) -> Unit =
  #| (self, children) => { if (children.length > 0) { self.add(...children); }; }

///|
/// Adds this object to the parent, retaining this object as the cascade receiver.
pub extern "js" fn ArrowHelper::add_to(
  self : ArrowHelper,
  parent : Object3D,
) -> Unit =
  #| (self, parent) => { parent.add(self); }

///|
/// Removes a child using native event behavior. Does not dispose resources.
pub extern "js" fn ArrowHelper::remove_child(
  self : ArrowHelper,
  child : Object3D,
) -> Unit =
  #| (self, child) => { self.remove(child); }
